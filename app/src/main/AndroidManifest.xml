<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/iconoapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconoapp"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicacionFinancieraV2"
        tools:targetApi="31">

        <activity
            android:name=".View.RecyclerView"
            android:exported="false" />

        <!-- Configuración para Welcome como launcher -->
        <activity
            android:name=".View.Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".View.Transaccion"
            android:exported="false" />
        <activity
            android:name=".View.Login"
            android:exported="false" />
        <activity
            android:name=".View.Voucher"
            android:exported="false" />
        <activity
            android:name=".View.Register"
            android:exported="false" />
        <activity
            android:name=".View.Home"
            android:exported="false" />
    </application>

</manifest>